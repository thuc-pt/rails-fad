<h1><%= t ".fad" %></h1>
<h2><%= t ".hello" %> <%= @order.name %></h1>
<h3><%= t ".thank" %></h3>
<% @order.order_items.each do |item| %>
  <%= item.quantity %> x <%= item.product.name %>
  <%= number_to_currency(item.price) %> = <%= number_to_currency(item.quantity * item.price) %>
  <br>
<% end %>
